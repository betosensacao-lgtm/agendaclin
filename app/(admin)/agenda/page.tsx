/**
 * Placeholder da agenda — implementação completa vem na F4 (visão diária,
 * filtros por profissional, status de cada booking).
 */
export default function AgendaPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold tracking-tight">Agenda</h1>
      <p className="text-muted-foreground">
        Visão diária/semanal das consultas — em construção (F4).
      </p>
    </div>
  );
}
